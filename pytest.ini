[pytest]
# Only collect THIS project's tests - never recurse into the vendored Archipelago tree
# (which ships thousands of its own tests), the venv, or build output.
testpaths = tests
norecursedirs = vendor .venv build dist .git tools/_attic
addopts = -q
